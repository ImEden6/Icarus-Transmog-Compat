package dev.mayaqq.estrogen.client.content.entityRenderers.mothElytra;

import net.minecraft.client.render.VertexConsumerProvider;
import net.minecraft.client.util.math.MatrixStack;
import net.minecraft.entity.LivingEntity;

/**
 * Stub class for Estrogen's MothElytraLayer.
 * This class only exists to provide symbols for the compiler and remapper (Loom).
 * It will NOT be included in the final mod JAR.
 */
public class MothElytraLayer {
    // Signature of the render method from Estrogen's source
    public void render(MatrixStack matrices, VertexConsumerProvider vertexConsumers, int light, LivingEntity entity, float limbAngle, float limbDistance, float tickDelta, float animationProgress, float headYaw, float headPitch) {
        // Stub
    }
}
